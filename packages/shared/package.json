{
  "name": "@agency/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc -w -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"No lint configured for shared\"",
    "test": "echo \"No tests configured for shared\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
